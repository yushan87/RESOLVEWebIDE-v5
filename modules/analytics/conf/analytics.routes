# Routes (Analytics)
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.analytics.Analytics.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               @controllers.analytics.Assets.versioned(path="/public", file)
