# Routes (Common)
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Registration page
GET     /registration               controllers.common.authentication.Registration.index()
POST    /registration               controllers.common.authentication.Registration.handleSubmit()

# Password Recovery page
GET     /passwordrecovery           controllers.common.authentication.PasswordRecovery.index()
POST    /passwordrecovery           controllers.common.authentication.PasswordRecovery.handleSubmit()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.common.Assets.versioned(path="/public", file)
